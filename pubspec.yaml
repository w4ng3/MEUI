name: meui
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.0 <3.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  pigeon: ^7.0.0

flutter:

  plugin:
    platforms:
      android:
        package: top.w2gd.meui
        pluginClass: MeuiPlugin
      ios:
        pluginClass: MeuiPlugin


